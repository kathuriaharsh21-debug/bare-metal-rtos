# CMSIS-RTOS2 Wrapper (`interop/cmsis/rtos2`)

STK provides a complete **CMSIS-RTOS2** compatibility layer (`cmsis_os2_stk.cpp`) that maps the standard ARM [CMSIS-RTOS2 C API](https://arm-software.github.io/CMSIS_6/latest/RTOS2/group__CMSIS__RTOS.html) (`cmsis_os2.h` v2.3.0) onto the STK C++ kernel. This allows you to use STK as a drop-in RTOS backend in any project that targets the CMSIS-RTOS2 interface, including code generated by STM32CubeMX, MCUXpresso, or any other CMSIS-aware IDE or middleware stack.

## Supported API Groups

| API Group         | Functions Covered                                                                                                                                                                                                                                                                                                                                  |
|-------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Kernel Management | `osKernelInitialize`, `osKernelStart`, `osKernelGetState`, `osKernelGetInfo`, `osKernelGetTickCount`, `osKernelGetTickFreq`, `osKernelGetSysTimerCount`, `osKernelGetSysTimerFreq`, `osKernelLock`, `osKernelUnlock`, `osKernelRestoreLock`                                                                                                        |
| Thread Management | `osThreadNew`, `osThreadDelete`, `osThreadYield`, `osDelay`, `osDelayUntil`, `osThreadGetId`, `osThreadGetName`, `osThreadGetState`, `osThreadGetPriority`, `osThreadSetPriority`, `osThreadGetStackSize`, `osThreadGetStackSpace`, `osThreadGetCount`, `osThreadTerminate`, `osThreadSuspend`, `osThreadResume`, `osThreadJoin`, `osThreadDetach` |
| Thread Flags      | `osThreadFlagsSet`, `osThreadFlagsClear`, `osThreadFlagsGet`, `osThreadFlagsWait`                                                                                                                                                                                                                                                                  |
| Event Flags       | `osEventFlagsNew`, `osEventFlagsDelete`, `osEventFlagsSet`, `osEventFlagsClear`, `osEventFlagsGet`, `osEventFlagsWait`                                                                                                                                                                                                                             |
| Mutex             | `osMutexNew`, `osMutexDelete`, `osMutexAcquire`, `osMutexRelease`, `osMutexGetOwner`                                                                                                                                                                                                                                                               |
| Semaphore         | `osSemaphoreNew`, `osSemaphoreDelete`, `osSemaphoreAcquire`, `osSemaphoreRelease`, `osSemaphoreGetCount`                                                                                                                                                                                                                                           |
| Timer             | `osTimerNew`, `osTimerDelete`, `osTimerStart`, `osTimerStop`, `osTimerIsRunning`                                                                                                                                                                                                                                                                   |
| Message Queue     | `osMessageQueueNew`, `osMessageQueueDelete`, `osMessageQueuePut`, `osMessageQueueGet`, `osMessageQueueGetCapacity`, `osMessageQueueGetMsgSize`, `osMessageQueueGetCount`, `osMessageQueueGetSpace`, `osMessageQueueReset`                                                                                                                          |
| Memory Pool       | `osMemoryPoolNew`, `osMemoryPoolDelete`, `osMemoryPoolAlloc`, `osMemoryPoolFree`, `osMemoryPoolGetCapacity`, `osMemoryPoolGetBlockSize`, `osMemoryPoolGetCount`, `osMemoryPoolGetSpace`, `osMemoryPoolGetName`                                                                                                                                     |

## Design & Mapping

* **Kernel configuration:** The wrapper instantiates one global `stk::Kernel` with `KERNEL_DYNAMIC | KERNEL_SYNC` flags and the `SwitchStrategyFP32` (32-level fixed-priority) scheduler. Tickless mode is enabled automatically when `STK_TICKLESS_IDLE=1` is defined.
* **Thread capacity:** Controlled by the `CMSIS_STK_MAX_THREADS` macro (default: 16). Increase it if more concurrent threads are needed.
* **Priority mapping:** CMSIS priorities (`osPriorityIdle=1` … `osPriorityISR=56`) are linearly mapped to STK FP32 priority levels 0–31: `stk_prio = (cmsis_prio × 31) / 56`.
* **Timeouts:** CMSIS tick values are passed through directly as STK ticks (1:1 when tick resolution is 1 ms / `PERIODICITY_DEFAULT`). `osWaitForever` is translated to `stk::WAIT_INFINITE`.
* **Thread flags & event flags:** Backed by `stk::sync::EventFlags` - STK's native 32-bit multi-flag synchronization primitive with ISR-safe `Set`/`Clear` and `ANY`/`ALL`/`NO_CLEAR` wait options.
* **Message queues:** Backed by `stk::sync::MessageQueue` - STK's native fixed-capacity, fixed-message-size ring-buffer with integrated blocking `Put`/`Get`, ISR-safe `TryPut`/`TryGet`, and configurable timeouts.
* **Memory pools:** Backed by `stk::memory::BlockMemoryPool` - STK's O(1), zero-fragmentation fixed-block allocator.
* **Timers:** Backed by `stk::time::TimerHost`. A single global `TimerHost` is created on the first `osTimerNew()` call.
* **Static vs. heap allocation:** All CMSIS objects support the standard `cb_mem` / `cb_size` placement-new path (zero heap usage when caller provides memory), falling back to `operator new` when no buffer is supplied.
* **Stack memory:** Thread stacks can be caller-supplied (`attr->stack_mem`) or heap-allocated. The default stack size is `CMSIS_STK_DEFAULT_STACK_WORDS` (default: 256 words).

## Known Limitations

| Area                                              | Behaviour                                                                                    |
|---------------------------------------------------|----------------------------------------------------------------------------------------------|
| Priority inheritance (`osMutexPrioInherit`)       | Ignored. STK supports it automatically for `SwitchStrategyFixedPriority/SwitchStrategyFP32`. |
| Robust mutex (`osMutexRobust`)                    | Ignored as unsuported. Will assert in Debug build as unsafe coding practice.                 |
| Message priority (`osMessageQueuePut` `msg_prio`) | Ignored. STK queues are FIFO with no priority lanes.                                         |
| SMP affinity (`osThreadSetAffinityMask`)          | Ignored. STK uses a per-core AMP model.                                                      |

---

## Example

There are working examples for STM32F407G-DISC1 development board:

- [Advanced Blinky](https://github.com/SuperTinyKernel-RTOS/stk/tree/main/build/example/project/eclipse/stm/blinky-cmsis-rtos2-stm32f407g-disc1/src//main.c)
- [Priority Inversion](https://github.com/SuperTinyKernel-RTOS/stk/blob/main/build/example/project/eclipse/stm/blinky-cmsis-rtos2-prio-stm32f407g-disc1/src/main.c)

---

## Quick Integration

### 1. Add the wrapper source to your build

```make
SRCS += libs/stk/interop/cmsis/rtos2/cmsis_os2_stk.cpp
```

### 2. Add include paths

```make
INCLUDES += -Ilibs/stk/include
INCLUDES += -Ilibs/stk/interop/cmsis/rtos2
```

### 3. (Optional) Override defaults in your `stk_config.h`

```cpp
// Maximum number of concurrent CMSIS threads (default: 16)
#define CMSIS_STK_MAX_THREADS         32U

// Default stack size in Words when osThreadAttr_t::stack_size == 0 (default: 256)
#define CMSIS_STK_DEFAULT_STACK_WORDS 512U
```

### 4. Initialize and start the kernel

```c
#include "cmsis_os2.h"

int main(void)
{
    // Board / clock init here ...

    osKernelInitialize();

    osThreadNew(app_thread, NULL, NULL);  // create at least one thread

    osKernelStart();                      // never returns
}
```

All subsequent CMSIS-RTOS2 calls (`osThreadNew`, `osMutexNew`, `osMessageQueueNew`, etc.) work as documented in the CMSIS-RTOS2 specification.